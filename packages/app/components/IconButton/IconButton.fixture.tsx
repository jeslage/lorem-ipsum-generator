import IconButton from "./IconButton";

export default (
  <div>
    <IconButton icon="copy" label="Settings" />
    <IconButton icon="dots" variant="outlined" label="Settings" />
    <IconButton icon="copy" disabled label="Settings" />
    <IconButton icon="dots" disabled variant="outlined" label="Settings" />
  </div>
);
